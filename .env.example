# Backend environment variables. Export these in your shell or host settings.
RECORD_LIFE_DB=/data/record_life.duckdb
RECORD_LIFE_ORIGINS=https://your-app.vercel.app
# Required by `python -m api.serve` when host is not loopback.
RECORD_LIFE_API_TOKEN=replace-with-a-long-random-secret
RECORD_LIFE_HOST=0.0.0.0
PORT=8000
